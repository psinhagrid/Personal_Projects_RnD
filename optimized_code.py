import os
import sys
import re
import numpy as np
import pandas as pd
def c(a,b):return a+b
def f(a):return[b*2if b>0else 0for b in a]if a else None
l=lambda n:[sum(n[:j]+n[j+1:])for j in range(len(n))]
class SimpleDataProcessor:
def __init__(a,b):a.c,a.d=[b,[]];def e(a,f):a.d.append(f);return True
def g(a):return[h*2 if isinstance(h,(int,float))else str(h)for h in a.d if h is not None]
def __init__(s,a):s.b,s.c=[a,[]
def a(b,c):b.d.append(c);return True
a=[];[a.append(b*2)if isinstance(b,(int,float))else a.append(str(b))for b in self.data_list if b is not None];a
a,b,c=2,1000,True
if__name__=="__main__":a=b(c);a.d(1);a.d(2);a.d(3)
r=p.p_a_d();print(f"Processed data: {r}")